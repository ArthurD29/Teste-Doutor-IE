name: app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  asuka: ^2.0.0
  flutter_bloc: ^8.0.0
  flutter:
    sdk: flutter




dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.5

flutter:
  uses-material-design: true

  assets:
    - assets/images/